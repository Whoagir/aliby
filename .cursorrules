# Правила разработки для AI ассистента

## 🚫 КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНО

### Docker
- ❌ НИКОГДА не использовать `docker system prune` или подобные команды
- ❌ НИКОГДА не удалять volumes командами типа `docker volume rm`
- ❌ НИКОГДА не использовать `docker-compose down -v` (флаг -v удаляет volumes!)
- ❌ НЕ предлагать команды которые могут затронуть другие Docker проекты

### Git
- ❌ НЕ коммитить и не пушить без явного разрешения пользователя
- ❌ НЕ использовать `git push --force` без явного разрешения
- ❌ ВСЕГДА проверять `git remote -v` перед push
- ❌ НЕ добавлять git remote автоматически

### Файлы
- ❌ НЕ удалять файлы без явного разрешения
- ❌ НЕ перезаписывать важные конфиги (.env, credentials, и т.д.)
- ❌ **КРИТИЧНО**: НИКОГДА НЕ ПЕРЕЗАПИСЫВАТЬ `backend/app/data/words_alias.json` и `backend/app/data/words_taboo.json`
- ❌ **КРИТИЧНО**: Эти файлы содержат 25000+ слов! Для регенерации использовать ТОЛЬКО `backend/generate_word_db.py`
- ❌ Создавай backup перед любыми операциями с файлами слов

## ✅ ОБЯЗАТЕЛЬНО СОБЛЮДАТЬ

### Код и коммуникация
- ✅ БЕЗ смайликов в коде (README.md, комментарии, коммиты)
- ✅ БЕЗ эмодзи в коде и документации
- ✅ Лаконичный стиль в README
- ✅ Простые и понятные комментарии на английском

### Docker изоляция
- ✅ ВСЕГДА использовать `name:` в docker-compose.yml (например: `name: aliby-game`)
- ✅ ВСЕГДА использовать уникальные префиксы для volumes (например: `aliby_postgres_data`)
- ✅ ВСЕГДА использовать уникальные имена контейнеров
- ✅ НЕ трогать volumes других проектов

### Git workflow
- ✅ ВСЕГДА проверять текущий remote перед любыми git операциями
- ✅ ВСЕГДА спрашивать разрешение перед commit/push
- ✅ ВСЕГДА показывать что будет закоммичено (`git status`, `git diff`)

### Безопасность
- ✅ НЕ коммитить секреты (.env, ключи, пароли)
- ✅ Предупреждать если пользователь случайно показал приватный ключ/пароль
- ✅ Использовать .gitignore для чувствительных файлов

### Разработка
- ✅ Разработка ТОЛЬКО локально (не на проде)
- ✅ Hot reload для быстрой разработки
- ✅ Деплой на прод только после явного разрешения

## 📝 Примеры

### ❌ ПЛОХО
```yaml
# docker-compose.yml без name - может конфликтовать!
services:
  postgres:
    volumes:
      - postgres_data:/var/lib/postgresql/data  # Общее имя!
```

```bash
# Опасные команды
docker system prune -a
docker volume prune
docker-compose down -v
```

### ✅ ХОРОШО
```yaml
# docker-compose.yml с name и уникальными volumes
name: aliby-game

services:
  postgres:
    volumes:
      - aliby_postgres_data:/var/lib/postgresql/data

volumes:
  aliby_postgres_data:
  aliby_redis_data:
```

```bash
# Безопасные команды
docker compose ps
docker compose logs
docker compose restart
```

## 🎯 Приоритеты

1. **Безопасность** - не навредить существующим проектам
2. **Изоляция** - каждый проект в своем контуре
3. **Ясность** - всегда объяснять что делает команда
4. **Разрешение** - спрашивать перед деструктивными операциями

## 💬 Стиль коммуникации

- Лаконично и по делу
- Без лишних восторгов
- Без смайликов в коде/документации (в общении можно умеренно)
- Четкие инструкции с примерами
- Предупреждения о рисках

## 📊 Перед любой командой

1. Объяснить что делает команда
2. Показать возможные риски
3. Спросить подтверждение если команда деструктивная
4. Проверить изоляцию (не затронет ли другие проекты)
